--[[ =============================================
     MM2 PANEL v5 - ARREGLADO
     ============================================= ]]
warn("[MM2 Panel v5] Iniciando...")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")

local player = Players.LocalPlayer
local camera = workspace.CurrentCamera

warn("[MM2 Panel v5] Servicios cargados")

-- ========================================
--           CONFIG
-- ========================================
local Config = {
    ESP = false,
    ESPDistancia = false,
    ESPCaja2D = false,
    ESPTracer = false,
    ESPArmaMano = false,
    ESPArmas = false,
    AlertaMurder = false,
    AntiFling = false,
    Speed = false,
    Jump = false,
    Noclip = false,
    Fullbright = false,
}

local SPEED_VALUE = 50
local originalSpeed = 16

local State = {
    ESPObjects = {},
    ESPArmasObjects = {},
    ESPLoop = false,
    ArmasLoop = false,
    AlertaLoop = false,
    JumpConn = nil,
    NoclipConn = nil,
    AntiFlingConn = nil,
    Minimized = false,
}

local ROLE_COLORS = {
    Murderer = Color3.fromRGB(255, 40, 40),
    Sheriff  = Color3.fromRGB(40, 130, 255),
    Innocent = Color3.fromRGB(40, 220, 90),
    Unknown  = Color3.fromRGB(180, 180, 180),
}

-- ========================================
--           GUI BASE
-- ========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MM2_Panel"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = player:WaitForChild("PlayerGui")

warn("[MM2 Panel v5] ScreenGui creado")

-- Botón abrir
local OpenBtn = Instance.new("TextButton")
OpenBtn.Size = UDim2.new(0, 50, 0, 50)
OpenBtn.Position = UDim2.new(0, 15, 0.45, 0)
OpenBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
OpenBtn.TextColor3 = Color3.fromRGB(0, 220, 255)
OpenBtn.Text = "⚙"
OpenBtn.Font = Enum.Font.GothamBold
OpenBtn.TextSize = 24
OpenBtn.BorderSizePixel = 0
OpenBtn.Active = true
OpenBtn.Draggable = true
OpenBtn.Parent = ScreenGui

local openCorner = Instance.new("UICorner")
openCorner.CornerRadius = UDim.new(1, 0)
openCorner.Parent = OpenBtn

local openStroke = Instance.new("UIStroke")
openStroke.Color = Color3.fromRGB(0, 220, 255)
openStroke.Thickness = 2
openStroke.Parent = OpenBtn

-- Panel
local Panel = Instance.new("Frame")
Panel.Size = UDim2.new(0, 390, 0, 340)
Panel.Position = UDim2.new(0.5, -195, 0.5, -170)
Panel.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
Panel.BorderSizePixel = 0
Panel.Visible = false
Panel.Active = true
Panel.Draggable = true
Panel.Parent = ScreenGui

local panelCorner = Instance.new("UICorner")
panelCorner.CornerRadius = UDim.new(0, 12)
panelCorner.Parent = Panel

local panelStroke = Instance.new("UIStroke")
panelStroke.Color = Color3.fromRGB(0, 220, 255)
panelStroke.Thickness = 1.5
panelStroke.Transparency = 0.4
panelStroke.Parent = Panel

-- Header
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 34)
Header.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
Header.BorderSizePixel = 0
Header.Parent = Panel

local headerCorner = Instance.new("UICorner")
headerCorner.CornerRadius = UDim.new(0, 12)
headerCorner.Parent = Header

local headerFix = Instance.new("Frame")
headerFix.Size = UDim2.new(1, 0, 0, 12)
headerFix.Position = UDim2.new(0, 0, 1, -12)
headerFix.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
headerFix.BorderSizePixel = 0
headerFix.Parent = Header

local TitleLbl = Instance.new("TextLabel")
TitleLbl.Size = UDim2.new(1, -100, 1, 0)
TitleLbl.Position = UDim2.new(0, 12, 0, 0)
TitleLbl.BackgroundTransparency = 1
TitleLbl.Text = "🎮 MM2 PANEL v5"
TitleLbl.TextColor3 = Color3.fromRGB(0, 220, 255)
TitleLbl.Font = Enum.Font.GothamBold
TitleLbl.TextSize = 13
TitleLbl.TextXAlignment = Enum.TextXAlignment.Left
TitleLbl.Parent = Header

local MinBtn = Instance.new("TextButton")
MinBtn.Size = UDim2.new(0, 24, 0, 24)
MinBtn.Position = UDim2.new(1, -60, 0.5, -12)
MinBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 75)
MinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinBtn.Text = "−"
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 14
MinBtn.BorderSizePixel = 0
MinBtn.Parent = Header

local minCorner = Instance.new("UICorner")
minCorner.CornerRadius = UDim.new(1, 0)
minCorner.Parent = MinBtn

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 24, 0, 24)
CloseBtn.Position = UDim2.new(1, -30, 0.5, -12)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 60)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Text = "✕"
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 12
CloseBtn.BorderSizePixel = 0
CloseBtn.Parent = Header

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(1, 0)
closeCorner.Parent = CloseBtn

-- Grid de items
local ItemsHolder = Instance.new("ScrollingFrame")
ItemsHolder.Name = "ItemsHolder"
ItemsHolder.Size = UDim2.new(1, -16, 1, -46)
ItemsHolder.Position = UDim2.new(0, 8, 0, 40)
ItemsHolder.BackgroundTransparency = 1
ItemsHolder.BorderSizePixel = 0
ItemsHolder.ScrollBarThickness = 3
ItemsHolder.ScrollBarImageColor3 = Color3.fromRGB(0, 220, 255)
ItemsHolder.CanvasSize = UDim2.new(0, 0, 0, 0)
ItemsHolder.AutomaticCanvasSize = Enum.AutomaticSize.Y
ItemsHolder.Parent = Panel

local ItemsLayout = Instance.new("UIGridLayout")
ItemsLayout.CellSize = UDim2.new(0, 180, 0, 46)
ItemsLayout.CellPadding = UDim2.new(0, 6, 0, 6)
ItemsLayout.SortOrder = Enum.SortOrder.LayoutOrder
ItemsLayout.Parent = ItemsHolder

warn("[MM2 Panel v5] GUI base creada")

-- ========================================
--           FUNCIONES UI
-- ========================================
local function crearItem(nombre, emoji, callback)
    local item = Instance.new("TextButton")
    item.BackgroundColor3 = Color3.fromRGB(28, 28, 36)
    item.Text = ""
    item.BorderSizePixel = 0
    item.AutoButtonColor = false
    item.Parent = ItemsHolder
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = item
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(60, 60, 75)
    stroke.Thickness = 1.5
    stroke.Parent = item
    
    local emojiLbl = Instance.new("TextLabel")
    emojiLbl.Size = UDim2.new(0, 22, 0, 20)
    emojiLbl.Position = UDim2.new(0, 8, 0, 4)
    emojiLbl.BackgroundTransparency = 1
    emojiLbl.Text = emoji
    emojiLbl.TextSize = 15
    emojiLbl.Font = Enum.Font.GothamBold
    emojiLbl.TextXAlignment = Enum.TextXAlignment.Left
    emojiLbl.Parent = item
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -14, 0, 14)
    lbl.Position = UDim2.new(0, 8, 1, -17)
    lbl.BackgroundTransparency = 1
    lbl.Text = nombre
    lbl.TextColor3 = Color3.fromRGB(220, 220, 230)
    lbl.Font = Enum.Font.GothamSemibold
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = item
    
    local switch = Instance.new("Frame")
    switch.Size = UDim2.new(0, 30, 0, 15)
    switch.Position = UDim2.new(1, -38, 0, 6)
    switch.BackgroundColor3 = Color3.fromRGB(50, 50, 65)
    switch.BorderSizePixel = 0
    switch.Parent = item
    
    local switchCorner = Instance.new("UICorner")
    switchCorner.CornerRadius = UDim.new(1, 0)
    switchCorner.Parent = switch
    
    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 11, 0, 11)
    knob.Position = UDim2.new(0, 2, 0.5, -5.5)
    knob.BackgroundColor3 = Color3.new(1, 1, 1)
    knob.BorderSizePixel = 0
    knob.Parent = switch
    
    local knobCorner = Instance.new("UICorner")
    knobCorner.CornerRadius = UDim.new(1, 0)
    knobCorner.Parent = knob
    
    local isOn = false
    
    local function setState(state)
        isOn = state
        if isOn then
            switch.BackgroundColor3 = Color3.fromRGB(0, 220, 255)
            knob.Position = UDim2.new(1, -13, 0.5, -5.5)
            stroke.Color = Color3.fromRGB(0, 220, 255)
            lbl.TextColor3 = Color3.fromRGB(0, 220, 255)
        else
            switch.BackgroundColor3 = Color3.fromRGB(50, 50, 65)
            knob.Position = UDim2.new(0, 2, 0.5, -5.5)
            stroke.Color = Color3.fromRGB(60, 60, 75)
            lbl.TextColor3 = Color3.fromRGB(220, 220, 230)
        end
        if callback then
            pcall(callback, isOn)
        end
    end
    
    item.MouseButton1Click:Connect(function()
        setState(not isOn)
    end)
    
    return setState
end

local function crearBoton(nombre, emoji, callback)
    local item = Instance.new("TextButton")
    item.BackgroundColor3 = Color3.fromRGB(35, 28, 42)
    item.Text = ""
    item.BorderSizePixel = 0
    item.AutoButtonColor = false
    item.Parent = ItemsHolder
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = item
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(140, 80, 200)
    stroke.Thickness = 1.5
    stroke.Parent = item
    
    local emojiLbl = Instance.new("TextLabel")
    emojiLbl.Size = UDim2.new(0, 22, 0, 20)
    emojiLbl.Position = UDim2.new(0, 8, 0, 4)
    emojiLbl.BackgroundTransparency = 1
    emojiLbl.Text = emoji
    emojiLbl.TextSize = 15
    emojiLbl.Font = Enum.Font.GothamBold
    emojiLbl.TextXAlignment = Enum.TextXAlignment.Left
    emojiLbl.Parent = item
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -40, 0, 14)
    lbl.Position = UDim2.new(0, 8, 1, -17)
    lbl.BackgroundTransparency = 1
    lbl.Text = nombre
    lbl.TextColor3 = Color3.fromRGB(220, 200, 255)
    lbl.Font = Enum.Font.GothamSemibold
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = item
    
    local arrow = Instance.new("TextLabel")
    arrow.Size = UDim2.new(0, 20, 0, 20)
    arrow.Position = UDim2.new(1, -28, 0.5, -10)
    arrow.BackgroundTransparency = 1
    arrow.Text = "▶"
    arrow.TextColor3 = Color3.fromRGB(140, 80, 200)
    arrow.TextSize = 14
    arrow.Font = Enum.Font.GothamBold
    arrow.Parent = item
    
    item.MouseButton1Click:Connect(function()
        if callback then pcall(callback) end
    end)
    
    return item
end

warn("[MM2 Panel v5] Funciones UI listas")

-- ========================================
--           ROLES
-- ========================================
local function getRole(targetPlayer)
    local char = targetPlayer.Character
    if not char then return "Unknown" end
    local backpack = targetPlayer:FindFirstChild("Backpack")
    
    local function hasTool(name)
        if char:FindFirstChild(name) then return true end
        if backpack and backpack:FindFirstChild(name) then return true end
        return false
    end
    
    if hasTool("Knife") then return "Murderer" end
    if hasTool("Gun") or hasTool("Revolver") then return "Sheriff" end
    return "Innocent"
end

local function getRoleEmoji(role)
    if role == "Murderer" then return "🔪" end
    if role == "Sheriff" then return "🔫" end
    if role == "Innocent" then return "😇" end
    return "❓"
end

-- ========================================
--           ESP
-- ========================================
local function removeESP(targetPlayer)
    if State.ESPObjects[targetPlayer] then
        for _, obj in pairs(State.ESPObjects[targetPlayer]) do
            if obj and obj.Destroy then
                pcall(function() obj:Destroy() end)
            end
        end
        State.ESPObjects[targetPlayer] = nil
    end
end

local function createESP(targetPlayer)
    if targetPlayer == player then return end
    local char = targetPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    removeESP(targetPlayer)
    State.ESPObjects[targetPlayer] = {}
    local objs = State.ESPObjects[targetPlayer]
    
    local hl = Instance.new("Highlight")
    hl.FillTransparency = 0.5
    hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Adornee = char
    hl.Parent = char
    objs.Highlight = hl
    
    local head = char:FindFirstChild("Head")
    if head then
        local bb = Instance.new("BillboardGui")
        bb.Size = UDim2.new(0, 220, 0, 42)
        bb.StudsOffset = Vector3.new(0, 2.8, 0)
        bb.AlwaysOnTop = true
        bb.Parent = head
        
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, 0, 0.6, 0)
        lbl.BackgroundTransparency = 1
        lbl.TextColor3 = Color3.new(1, 1, 1)
        lbl.TextStrokeTransparency = 0
        lbl.TextStrokeColor3 = Color3.new(0, 0, 0)
        lbl.Font = Enum.Font.GothamBold
        lbl.TextSize = 13
        lbl.Text = ""
        lbl.Parent = bb
        
        local dist = Instance.new("TextLabel")
        dist.Size = UDim2.new(1, 0, 0.4, 0)
        dist.Position = UDim2.new(0, 0, 0.6, 0)
        dist.BackgroundTransparency = 1
        dist.TextColor3 = Color3.fromRGB(0, 220, 255)
        dist.TextStrokeTransparency = 0
        dist.TextStrokeColor3 = Color3.new(0, 0, 0)
        dist.Font = Enum.Font.GothamSemibold
        dist.TextSize = 11
        dist.Text = ""
        dist.Parent = bb
        
        objs.Billboard = bb
        objs.Label = lbl
        objs.DistLabel = dist
    end
    
    local boxBB = Instance.new("BillboardGui")
    boxBB.Size = UDim2.new(0, 70, 0, 90)
    boxBB.StudsOffset = Vector3.new(0, 1.5, 0)
    boxBB.AlwaysOnTop = true
    boxBB.Adornee = hrp
    boxBB.Enabled = false
    boxBB.Parent = hrp
    
    local boxFrame = Instance.new("Frame")
    boxFrame.Size = UDim2.new(1, 0, 1, 0)
    boxFrame.BackgroundTransparency = 1
    boxFrame.BorderSizePixel = 2
    boxFrame.BorderColor3 = Color3.fromRGB(255, 255, 255)
    boxFrame.Parent = boxBB
    
    objs.Box2D = boxBB
    objs.Box2DFrame = boxFrame
    
    local tracer = Instance.new("Frame")
    tracer.BackgroundColor3 = Color3.fromRGB(0, 220, 255)
    tracer.BorderSizePixel = 0
    tracer.AnchorPoint = Vector2.new(0, 0.5)
    tracer.ZIndex = 2
    tracer.Visible = false
    tracer.Parent = ScreenGui
    objs.Tracer = tracer
end

local function updateESP(targetPlayer)
    if targetPlayer == player then return end
    local char = targetPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    if not State.ESPObjects[targetPlayer] then
        createESP(targetPlayer)
    end
    local objs = State.ESPObjects[targetPlayer]
    if not objs then return end
    
    local role = getRole(targetPlayer)
    local color = ROLE_COLORS[role]
    
    if objs.Highlight then
        objs.Highlight.FillColor = color
        objs.Highlight.OutlineColor = color
    end
    
    if objs.Label then
        objs.Label.Text = getRoleEmoji(role) .. " " .. targetPlayer.Name
        objs.Label.TextColor3 = color
    end
    
    if objs.DistLabel then
        if Config.ESPDistancia then
            local dist = math.floor((camera.CFrame.Position - hrp.Position).Magnitude)
            objs.DistLabel.Text = "[" .. dist .. "m]"
        else
            objs.DistLabel.Text = ""
        end
    end
    
    if objs.Box2D then
        if Config.ESPCaja2D then
            objs.Box2D.Enabled = true
            objs.Box2DFrame.BorderColor3 = color
        else
            objs.Box2D.Enabled = false
        end
    end
    
    -- Tracer ARREGLADO (usa ViewportSize en vez de ScreenGui.AbsoluteSize)
    if objs.Tracer then
        if Config.ESPTracer then
            local screenPos, onScreen = camera:WorldToViewportPoint(hrp.Position)
            if onScreen and screenPos.Z > 0 then
                local vp = camera.ViewportSize
                local originX = vp.X * 0.5
                local originY = vp.Y - 20
                local targetX = screenPos.X
                local targetY = screenPos.Y
                local dx = targetX - originX
                local dy = targetY - originY
                local length = math.sqrt(dx*dx + dy*dy)
                local angle = math.deg(math.atan2(dy, dx))
                
                objs.Tracer.Position = UDim2.new(0, originX, 0, originY)
                objs.Tracer.Size = UDim2.new(0, length, 0, 1.5)
                objs.Tracer.Rotation = angle
                objs.Tracer.BackgroundColor3 = color
                objs.Tracer.Visible = true
            else
                objs.Tracer.Visible = false
            end
        else
            objs.Tracer.Visible = false
        end
    end
    
    -- Arma en mano
    if Config.ESPArmaMano then
        local handTool = nil
        for _, child in ipairs(char:GetChildren()) do
            if child:IsA("Tool") then
                local n = child.Name:lower()
                if n == "knife" or n == "gun" or n == "revolver" then
                    handTool = child
                    break
                end
            end
        end
        
        if handTool and handTool:FindFirstChild("Handle") then
            if not objs.HandWeaponHL or not objs.HandWeaponHL.Parent then
                local hwl = Instance.new("Highlight")
                local n = handTool.Name:lower()
                local wColor = n:find("knife") and Color3.fromRGB(255, 140, 0) or Color3.fromRGB(255, 220, 0)
                hwl.FillColor = wColor
                hwl.OutlineColor = wColor
                hwl.FillTransparency = 0.3
                hwl.OutlineTransparency = 0
                hwl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                hwl.Adornee = handTool
                hwl.Parent = handTool
                objs.HandWeaponHL = hwl
            end
        else
            if objs.HandWeaponHL and objs.HandWeaponHL.Parent then
                pcall(function() objs.HandWeaponHL:Destroy() end)
            end
            objs.HandWeaponHL = nil
        end
    else
        if objs.HandWeaponHL and objs.HandWeaponHL.Parent then
            pcall(function() objs.HandWeaponHL:Destroy() end)
        end
        objs.HandWeaponHL = nil
    end
end

local function clearESP()
    for p, _ in pairs(State.ESPObjects) do
        removeESP(p)
    end
    State.ESPObjects = {}
end

local function startESPLoop()
    if State.ESPLoop then return end
    State.ESPLoop = true
    task.spawn(function()
        while Config.ESP or Config.ESPCaja2D or Config.ESPTracer or Config.ESPArmaMano or Config.ESPDistancia do
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= player then
                    pcall(function() updateESP(p) end)
                end
            end
            task.wait(0.15)
        end
        clearESP()
        State.ESPLoop = false
    end)
end

-- ========================================
--           ESP ARMAS TIRADAS
-- ========================================
local function isWeaponTool(obj)
    if not obj:IsA("Tool") then return false end
    local n = obj.Name:lower()
    return n == "knife" or n == "gun" or n == "revolver" or n:find("knife") or n:find("gun")
end

local function getWeaponColor(tool)
    local n = tool.Name:lower()
    if n:find("knife") then
        return Color3.fromRGB(255, 140, 0)
    end
    return Color3.fromRGB(255, 220, 0)
end

local function findDroppedWeapons()
    local weapons = {}
    local seen = {}
    
    local function scan(container)
        for _, obj in ipairs(container:GetChildren()) do
            if isWeaponTool(obj) and not seen[obj] and obj:FindFirstChild("Handle") then
                seen[obj] = true
                table.insert(weapons, obj)
            end
        end
    end
    
    local dropped = workspace:FindFirstChild("Dropped")
    if dropped then scan(dropped) end
    
    for _, container in ipairs(workspace:GetChildren()) do
        if container:IsA("Folder") and container.Name ~= "Dropped" then
            scan(container)
     
